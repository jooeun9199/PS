#include <stdio.h>

int main(){

    int n,q;

    scanf("%d %d", &n, &q);
    
    int price[n];

    for(int i=0;i<n;i++){
        scanf("%d", &price[i]);
    }

    for(int i=0;i<q;i++){
        scanf("%d %d", &x, &y);

        sum = 0;
        for (int j=0; j<k; j++){
            sum += 
        }
        printf("%d", aum)
    }

    return 0;
}